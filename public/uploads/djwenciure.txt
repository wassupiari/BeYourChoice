<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>materiale Studente</title>
     <link rel="stylesheet" href="{{ url_for('static', filename='css/stylematerialedidattico.css') }}">
</head>
<body>
{% include 'header.html'%}
<main>
        {% if materiali %}
            <div class="materiale-container">
                {% for materiale in materiali %}
                 <div class="materiale">
                {% if materiale['Tipo'] == 'video' %}
                <img src="/static/icons/video-icon.png" alt="Icona Video">
                {% elif materiale['Tipo'] == 'documento' %}
                <img src="/static/icons/document-icon.png" alt="Icona Documento">
                {% else %}
                <img src="/static/icons/file-icon.png" alt="Icona Generica">
                {% endif %}
                <h3>{{ materiale['Titolo'] }}</h3>
                <p>{{ materiale['Descrizione'] }}</p>
                <a href="{{ materiale['File_Path'] }}" target="_blank">Apri</a>
                     </div>
                {% endfor %}
            </div>
        {% else %}
            <p class="empty-message">Nessun file presente.</p>
        {% endif %}
    </main>
</body>
</html>

















<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <title>Materiale Studente</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='css/stylematerialedidattico.css') }}">
</head>
<body>
    {% include 'header.html' %}

    <main>
        {% if materiali %}
            <div class="materiali-lista">
                {% for materiale in materiali %}
                    <div class="materiale-item">
                        <h3>{{ materiale['Titolo'] }}</h3>
                        <p>{{ materiale['Descrizione'] }}</p>
                        <p><strong>Tipo:</strong> {{ materiale['Tipo'] }}</p>
                        <div class="azioni">
                            <!-- Visualizza il materiale con il link corretto -->
                            <a href="{{ url_for('serve_file', filename=materiale['File_Path'].split('/')[-1]) }}" target="_blank" class="btn">Visualizza</a>

                            <!-- Modifica il materiale -->
                            <a href="{{ url_for('modifica_materiale', id=materiale['ID_MaterialeDidattico']) }}" class="btn">Modifica</a>

                            <!-- Rimuovi il materiale -->
                            <a href="{{ url_for('rimuovi_materiale', id=materiale['ID_MaterialeDidattico']) }}" class="btn btn-danger" onclick="return confirm('Sei sicuro di voler eliminare questo materiale?');">Rimuovi</a>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <p class="empty-message">Nessun materiale presente.</p>
        {% endif %}
    </main>
</body>
</html>














<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifica Materiale Didattico</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='css/stylematerialedidattico.css') }}">
</head>
<body>
    {% include 'header.html' %}

    <main>
        <h2>Modifica Materiale Didattico</h2>

        {% if materiale %}
            <form action="{{ url_for('modifica_materiale', id=materiale['ID_MaterialeDidattico']) }}" method="POST">
                <div class="form-group">
                    <label for="titolo">Titolo</label>
                    <input type="text" id="titolo" name="titolo" value="{{ materiale['Titolo'] }}" required>
                </div>

                <div class="form-group">
                    <label for="descrizione">Descrizione</label>
                    <textarea id="descrizione" name="descrizione" required>{{ materiale['Descrizione'] }}</textarea>
                </div>

                <div class="form-group">
                    <label for="tipo">Tipo</label>
                    <select id="tipo" name="tipo" required>
                        <option value="documento" {% if materiale['Tipo'] == 'documento' %} selected {% endif %}>Documento</option>
                        <option value="video" {% if materiale['Tipo'] == 'video' %} selected {% endif %}>Video</option>
                        <option value="altro" {% if materiale['Tipo'] == 'altro' %} selected {% endif %}>Altro</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="file">Nuovo File (opzionale)</label>
                    <input type="file" id="file" name="file">
                </div>

                <button type="submit">Salva Modifiche</button>
                <a href="{{ url_for('visualizza_materiale_docente') }}" class="btn btn-cancel">Annulla</a>
            </form>
        {% else %}
            <p>Il materiale non è stato trovato.</p>
        {% endif %}
    </main>
</body>
</html>


























<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Caricamento Materiale Didattico</title>
    <link rel="stylesheet" href="/static/stylematerialedidattico.css">
</head>
{% include 'header.html'%}
<body>
    <header>
        <h1>Caricamento Materiale Didattico</h1>
    </header>
    <main>
        <form action="{{ url_for('carica_materiale') }}" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="titolo">Titolo del Materiale:</label>
                <input type="text" id="titolo" name="titolo" placeholder="Inserisci il titolo del materiale" required>
            </div>
            <div class="form-group">
                <label for="descrizione">Descrizione:</label>
                <textarea id="descrizione" name="descrizione" placeholder="Inserisci una breve descrizione" rows="4" required></textarea>
            </div>
            <div class="form-group">
                <label for="tipo">Tipo:</label>
                <select id="tipo" name="tipo" required>
                    <option value="documento">Documento</option>
                    <option value="video">Video</option>
                    <option value="altro">Altro</option>
                </select>
            </div>
            <div class="form-group">
                <label for="file">Seleziona il file:</label>
                <input type="file" id="file" name="file" required>
            </div>
            <button type="submit">Carica</button>
        </form>
    </main>
</body>
</html>
